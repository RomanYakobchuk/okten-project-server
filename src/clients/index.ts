import {googleClient, getGoogleUserInfo} from "./googleClient";
import {getFacebookUserInfo} from "./facebookClient";
import {getAccessTokenFromCode, getGitHubUserData} from "./githubClient";

export {
    googleClient,
    getGoogleUserInfo,
    getFacebookUserInfo,
    getAccessTokenFromCode,
    getGitHubUserData
}